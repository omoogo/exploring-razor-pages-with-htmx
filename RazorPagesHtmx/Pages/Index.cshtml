@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <button class="btn btn-primary mb-3" hx-get="/Index?handler=hello" hx-target="#message">Click Me!</button>
    <div id="message"></div>
</div>
